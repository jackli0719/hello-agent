# Beta 验收清单（v0.9.2）

> **本清单是 Beta 试用阶段的标准验收表**。试用者按 [docs/DEMO.md](DEMO.md) 跑演示流程，每项打勾。
> 试用前先跑 `npm run seed:demo` 重置演示数据（详见 README）。
> 详细反馈请填 [docs/FEEDBACK.md](FEEDBACK.md)。

---

## 试用环境

| 项                           | 内容                      |
| ---------------------------- | ------------------------- |
| 试用日期                     | 2026/**/**                |
| 试用时长                     | _____ 分钟                |
| Node 版本                    | `node -v` 输出 __________ |
| 是否跑了 `npm run seed:demo` | ☐ 是 ☐ 否                 |
| 是否跑了 `npm run build`     | ☐ 是 ☐ 否                 |
| 试用数据                     | demo（推荐）/ 自建        |

---

## 1. 用户端（C 端）

### 1.1 用户端下单

- **验收动作**：打开 `/customer` → 选品类「家政」→ 选 SKU「日常保洁 2 小时」→ 填姓名/手机/地址 → 提交
- **预期结果**：
  - 选类目后 SKU 下拉自动筛选
  - 提交后绿色提示「✓ 订单已提交」+ 显示订单号（O+YYYYMMDD+xxxx）
  - 提示「我们会尽快安排师傅与您联系」
- **是否通过**：☐ ✓ ☐ ✗（部分通过：________________）
- **问题记录**：________________________________________________________________

### 1.2 用户端查单

- **验收动作**：打开 `/customer/orders` → 输入 demo 账号 customer1 手机 `13900000099` → 查询
- **预期结果**：看到 customer1 名下的所有订单（pending / assigned / in_service / completed / cancelled 5 状态）
- **是否通过**：☐ ✓ ☐ ✗
- **问题记录**：________________________________________________________________

### 1.3 用户端订单详情

- **验收动作**：点击订单行 → 进入详情页 `/customer/orders/[id]`
- **预期结果**：看到完整字段（订单号、服务、金额、预约时间、分配师傅、备注、状态）
- **是否通过**：☐ ✓ ☐ ✗
- **问题记录**：________________________________________________________________

---

## 2. 后台（B 端）

### 2.1 后台订单筛选

- **验收动作**：登录 admin → `/orders` → 用状态 chip 切换「待派单 / 已派单 / 服务中 / 已完成 / 已取消」
- **预期结果**：
  - 默认显示 5 状态过滤 chip + 全部
  - 切状态后列表过滤正确
  - 默认 pageSize=10，底部有「上一页 / 下一页」
- **是否通过**：☐ ✓ ☐ ✗
- **问题记录**：________________________________________________________________

### 2.2 后台派单

- **验收动作**：`/orders?status=pending` → 找有「派给他」按钮的 pending 订单 → 点击
- **预期结果**：
  - 状态从「待派单」变「已派单」
  - 师傅列从「未派单」变师傅名字
  - `/masters` 看到该师傅 status 从 `available` 变 `busy`
- **是否通过**：☐ ✓ ☐ ✗
- **问题记录**：________________________________________________________________

### 2.3 后台状态流转

- **验收动作**：派单后 → 订单行内点「开始服务」→ 再点「完成订单」
- **预期结果**：
  - 「开始服务」→ 状态变「服务中」（蓝色徽标）
  - 「完成订单」→ 状态变「已完成」（绿色徽标）
  - 师傅 status 从 `busy` 变回 `available`
- **是否通过**：☐ ✓ ☐ ✗
- **问题记录**：________________________________________________________________

### 2.4 后台服务 / SKU 管理

- **验收动作**：`/services` → 看类目列表 → 点「新增品类」/「新增 SKU」→ 填表单提交
- **预期结果**：
  - 类目 code 强制大写（输 `clean` 自动变 `CLEAN`）
  - SKU basePrice ≥ 0（输 -1 报错）
  - SKU requiredSkills 可填逗号分隔
- **是否通过**：☐ ✓ ☐ ✗
- **问题记录**：________________________________________________________________

### 2.5 后台师傅管理

- **验收动作**：`/masters` → 点「新增师傅」→ 填表单提交
- **预期结果**：
  - 评分 0-5 校验（输 6 报错）
  - skills 必填（留空报错）
  - 手机号 11 位数字校验
  - 新师傅默认 status=available
- **是否通过**：☐ ✓ ☐ ✗
- **问题记录**：________________________________________________________________

### 2.6 后台派单规则管理

- **验收动作**：`/dispatch-rules` → 看规则列表 → 点「新增规则」→ 填 SKU 精确 / 品类兜底规则
- **预期结果**：
  - SKU 精确规则 priority 默认 100，品类兜底默认 50
  - 列表行内可切 enabled（停用后规则失效）
  - requiredSkills 非空校验
- **是否通过**：☐ ✓ ☐ ✗
- **问题记录**：________________________________________________________________

### 2.7 后台操作日志

- **验收动作**：`/activity-logs` → 看 14 种 action 类型 + actorRole / targetType 筛选 + 关键词搜索 + 分页
- **预期结果**：
  - 默认按 createdAt desc 排序（最新在最上）
  - actorRole 筛选 admin / worker / customer / system 4 类
  - keyword 搜 message 大小写不敏感
- **是否通过**：☐ ✓ ☐ ✗
- **问题记录**：________________________________________________________________

---

## 3. 师傅端（W 端）

### 3.1 师傅端订单分组

- **验收动作**：登录 worker1 / worker123 → 自动跳 `/worker` → 看订单按状态分组
- **预期结果**：
  - 4 个状态分组：待服务（assigned）/ 服务中 / 已完成 / 已取消
  - 每个分组有数量统计
  - 只显示当前 worker 绑定的师傅名下的订单
- **是否通过**：☐ ✓ ☐ ✗
- **问题记录**：________________________________________________________________

### 3.2 师傅端订单详情

- **验收动作**：点 assigned 订单的「查看详情 →」→ 跳到 `/worker/orders/[id]`
- **预期结果**：看到完整订单字段 + 「开始服务」蓝色大按钮
- **是否通过**：☐ ✓ ☐ ✗
- **问题记录**：________________________________________________________________

### 3.3 师傅开始服务

- **验收动作**：详情页点「开始服务」按钮
- **预期结果**：
  - 状态变「服务中」
  - 按钮变绿色「完成订单」
- **是否通过**：☐ ✓ ☐ ✗
- **问题记录**：________________________________________________________________

### 3.4 师傅完成服务

- **验收动作**：点「完成订单」→ 弹出 serviceSummary 输入框 → 填「清洗完毕」→ 提交
- **预期结果**：
  - 状态变「已完成」
  - serviceSummary 自动 trim 后写入 DB
  - 师傅 status 自动回 available
- **是否通过**：☐ ✓ ☐ ✗
- **问题记录**：________________________________________________________________

---

## 4. 取消 / 备注 / 服务说明

### 4.1 取消订单

- **验收动作**：选 pending 或 assigned 订单 → 点「取消订单」→ 弹表单 → 填取消原因 → 提交
- **预期结果**：
  - 状态变「已取消」
  - cancelReason 必填（留空报错「请填写取消原因」）
  - canceledAt 自动写当前时间
  - 师傅若有分配则自动释放回 available
- **是否通过**：☐ ✓ ☐ ✗
- **问题记录**：________________________________________________________________

### 4.2 备注 / 服务说明

- **验收动作**：
  - 用户下单：填「需要戴鞋套」→ 提交
  - 后台订单详情：看用户备注
  - 师傅完成：填 serviceSummary「清洗完毕，客户满意」
  - 后台订单详情：看 serviceSummary
- **预期结果**：两个字段都正确显示在订单详情页
- **是否通过**：☐ ✓ ☐ ✗
- **问题记录**：________________________________________________________________

---

## 5. Demo 数据重置

### 5.1 重置演示数据

- **验收动作**：`npm run seed:demo` → 看输出
- **预期结果**：
  - 输出 8 行成功提示（Category × 3 / SKU × 8 / Master × 4 / User × 7 / Order × 20 / Rule × 8 / ActivityLog × 10）
  - 重启 dev server 后 dashboard 统计正确（订单总数=20 / 待派单=8 / 已派单=4 / 服务中=4 / 已完成=3）
  - 三个测试账号（admin / customer1 / worker1）都能登录
- **是否通过**：☐ ✓ ☐ ✗
- **问题记录**：________________________________________________________________

---

## 验收汇总

| 模块      | 项数   | 通过 | 部分通过 | 未通过 |
| --------- | ------ | ---- | -------- | ------ |
| 用户端    | 3      | ___  | ___      | ___    |
| 后台      | 7      | ___  | ___      | ___    |
| 师傅端    | 4      | ___  | ___      | ___    |
| 取消/备注 | 2      | ___  | ___      | ___    |
| Demo 数据 | 1      | ___  | ___      | ___    |
| **合计**  | **17** | ___  | ___      | ___    |

---

## 试用反馈

填完本清单后，请继续填 [docs/FEEDBACK.md](FEEDBACK.md) 给整体试用感受 + 评分 + 推荐意愿。

试用完所有 17 项后，可以向项目维护者提交反馈，或开 GitHub Issue 讨论。
